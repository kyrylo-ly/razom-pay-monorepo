name: Notifications Service

on:
  push:
    branches: [main]
    paths:
      - "services/notifications/**"
      - "packages/core/**"
      - "packages/contracts/**"

  pull_request:
    branches: [main]
    paths:
      - "services/notifications/**"
      - "packages/core/**"
      - "packages/contracts/**"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      # enable when add tests
      # - run: pnpm --filter notifications test

      - run: pnpm --filter notifications... build

      - run: pnpm --filter notifications lint

  docker:
    needs: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/build-push-action@v6
        with:
          context: .
          file: services/notifications/Dockerfile
          push: true
          tags: ghcr.io/${{ github.repository }}/notifications:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
