apiVersion: 1

providers:
  - name: backend
    orgId: 1
    folder: Backend
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
